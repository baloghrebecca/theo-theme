//variables:
* {
    box-sizing: border-box;
}

@-webkit-keyframes ticker {
    0% {
        -webkit-transform: translate3d(0, 0, 0);
        transform: translate3d(0, 0, 0);
        visibility: visible;
    }
    100% {
        -webkit-transform: translate3d(-100%, 0, 0);
        transform: translate3d(-100%, 0, 0);
    }
}
@keyframes ticker {
    0% {
        -webkit-transform: translate3d(0, 0, 0);
        transform: translate3d(0, 0, 0);
        visibility: visible;
    }
    100% {
        -webkit-transform: translate3d(-100%, 0, 0);
        transform: translate3d(-100%, 0, 0);
    }
}

//IMPRESSUM
#impressum-body {
    color: #e6332a;
    background-color: #efe7de;
    padding: 30px;
    padding-top: 0;
    font-family: "Space Mono", monospace;
}

#impressum-password-container{
    width: 100%;
    padding: 30px;
    margin-top: 50px;
    display: none;
} 

.modal {
    display: none; /* Hidden by default */
    position: fixed; /* Stay in place */
    z-index: 9999999999999;
    left: 0;
    top: 0;
    width: 100%; /* Full width */
    height: 100%; /* Full height */
    overflow: auto; /* Enable scroll if needed */
    background-color: #efe7de;
  }

  /* Modal Content/Box */
.modal-content {
    margin: 0 auto; /* 15% from the top and centered */
    padding: 0 20px 20px 20px;
    width: 80%; /* Could be more or less, depending on screen size */
  }
  
  /* The Close Button */
  .close {
      margin-top: 0;
    text-align: end;
    color: #e6332a;
    font-size: 80px;
  }
  
  .close:hover,
  .close:focus {
    color: #702f8a;
    text-decoration: none;
    cursor: pointer;
  }

//Password Page

html {
    height: -webkit-fill-available;
}

.template-password {
    font-size: 18px;
    color: #e6332a;
    font-family: "Space Mono", monospace;
    background-image: url("../assets/001-2.png");
    background-size: cover;
    background-repeat: no-repeat;
    background-position: 0% 30%;
    margin: 0;
    height: 100vh; /* Fallback for browsers that do not support Custom Properties */
    height: calc(var(--vh, 1vh) * 100);
}

#password-section-container {
    height: 100vh; /* Fallback for browsers that do not support Custom Properties */
    height: calc(var(--vh, 1vh) * 100);
    // max-height: 100vh;
    width: 100%;
    /* mobile viewport bug fix */
    // min-height: -webkit-fill-available;
    display: flex;
    flex-direction: column;
    // flex: 1; 
    justify-content: space-between;
}

.password-content {
    padding-top: -150px;
    width: 100%;
}

.datenschutz {
    width: 100%;
    margin-top: -10px;
    margin-bottom: 15px;
    margin-left: 1px;
    font-size: 10px;
}

.password__field {
    width: 30%;

    input[type="submit"] {
        cursor: pointer;
    }
}

.counter {
    text-align: center;
    font-size: 120px;
    z-index: 100;
    width: 100%;
    position: fixed;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
}

.password-image {
    position: fixed;
    top: 18%;
    left: 50%;
    transform: translate(-50%, -50%);
    margin-top: 0px;
    height: 25vh;
    display: flex;
    width: 60vw;
}

a {
    color: #e6332a;
}

a:hover {
    color: #702f8a;
    text-decoration: none;
}

.password-image svg {
    // width: 40%;
    // height: 50%;
    // height: 100%;
}

//Liveticker

.ticker-wrap {
    margin-top: 10px;
    width: 100%;
    overflow: hidden;
    height: 4rem;
    box-sizing: content-box;
}
.ticker-wrap .ticker {
    display: inline-block;
    height: 4rem;
    line-height: 4rem;
    white-space: nowrap;
    padding-right: 100%;
    box-sizing: content-box;
    -webkit-animation-iteration-count: infinite;
    animation-iteration-count: infinite;
    -webkit-animation-timing-function: linear;
    animation-timing-function: linear;
    -webkit-animation-name: ticker;
    animation-name: ticker;
    -webkit-animation-duration: 1300s;
    animation-duration: 1300s;
}
.ticker-wrap .ticker__item {
    display: inline-block;
    padding: 0 2rem;
    font-weight: 800;
    color: #702f8a;
}

.newsticker-item {
    display: flex;

    p {
        margin-right: 40px;
    }
}

//FOOTER

.password-footer-newsletter-heading p {
    margin-bottom: 10px;
    width: 70%;
    font-weight: 600;
}

.password-footer-newsletter {
    z-index: 2;
}

.password-footer-social-credits {
    z-index: 2;
}

.password-footer {
    // position: relative;
    padding: 30px;
    z-index: 100;
    display: flex;
    align-items: flex-end;
    justify-content: space-between;
}

.blur {
    z-index: 0;
    position: absolute;
    z-index: 0;
    background: #efe7de;
    filter: blur(100px);
    height: 200px;
    bottom: 0;
    width: 100%;
}

.password-footer-newsletter-input-container {
    width: 100%;
}
input {
    color: #e6332a;
    font-size: 20px;
    font-family: "Space Mono", monospace;
    margin-bottom: 15px;
    border-radius: 8px;
    width: 100%;
    padding: 5px;
    border: 2px #e6332a solid;
    background-color: transparent;
}

input:focus {
    color: #702f8a;
    font-family: "Space Mono", monospace;
    border: 2px #702f8a solid;
    outline: none;
}

.password-footer-newsletter-email-send {
    margin-top: -5px;
    width: 100%;
    display: flex;
    justify-content: space-between;
}

button {
    border: none;
    margin: 0;
    padding: 0;
    cursor: pointer;
    color: #e6332a;
    font-size: 20px;
    font-family: "Space Mono", monospace;
    border: none;
    background-color: transparent;
    padding: 0;
}

button:hover {
    color: #702f8a;
}

button:focus {
    outline: none;
}

.password-footer-newsletter-email-send p {
    margin: 0;
}

.password-footer-social-credits {
    text-align: end;
}

.password-footer-credits {
    font-size: 20px;
    margin: 0;
    font-family: "Work Sans", sans-serif;
}

.password-footer-social-credits svg {
    margin-bottom: 5px;
}

.password-footer-impressum {
    margin: 0;
    font-size: 13px;
}

#mc_embed_signup,
.password-footer-newsletter {
    z-index: 200;
    width: 60%;
}


//Query:
@media (max-width: 800px) {
    .ticker-wrap {
        margin-top: 20px;
    }


    .password-footer-impressum {
        font-size: 10px;
    }

    .password-content {
        margin-top: -20px;
    }

    .template-password {
        font-size: 14px;
    }

    .password-image {
        position: fixed;
        top: 15%;
        left: 50%;
        transform: translate(-50%, -50%);
        margin-top: 0px;
        height: 20vh;
        display: flex;
        width: 60vw;
    }

    .counter {
        text-align: center;
        font-size: 70px;
        z-index: 100;
        width: 100%;
        position: fixed;
        top: 45%;
        left: 50%;
        transform: translate(-50%, -50%);
    }

    .password-footer {
        margin-top: -30px;
        position: relative;
        z-index: 100;
        flex-direction: column;
        align-items: center;
        justify-content: center;
    }

    form {
        width: 100%;
    }

    .password-footer-social-credits {
        width: 100%;
        z-index: 200;
        margin-top: 60px;
        display: flex;
        justify-content: space-between;
        align-items: center;
    }

    .blur {
        position: fixed;
        z-index: 0;
    }

    .password-footer-newsletter-email-send button {
        font-size: 15px;
    }

    input {
        padding: 10px;
        font-size: 15px;
    }

   


    .modal-content {
        margin: 0 auto;
        padding: 20px;
        width: 90%; /* Could be more or less, depending on screen size */
      }

    .password-footer-credits {
        font-size: 17px;
    }

    .password-footer-newsletter-heading p {
        width: 100%;
    }

    #mc_embed_signup,
    .password-footer-newsletter {
        z-index: 200;
        width: 100%;
    }

    @media (max-width: 550px) {
        .counter {
            text-align: center;
            font-size: 40px;
            z-index: 100;
            width: 100%;
            position: fixed;
            top: 40%;
            left: 50%;
            transform: translate(-50%, -50%);
        }
    }
}
